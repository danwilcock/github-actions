name: Vars

on: 
  workflow_dispatch:
  push:
    branches:
      - main

env: 
  bloop: test

jobs:
  run-git-action:
    runs-on: ubuntu-latest
    steps:
      - name: bash
        run: |
          echo ${bloop}