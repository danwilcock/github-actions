name: Vars

on: 
  workflow_dispatch:
  push:
    branches:
      - main

env: 
  bloop: test

jobs:
  run-git-action:
    runs-on: ubuntu-latest
    env: 
      gloop: fig
      bloop: test2
    steps:
      - name: bash
        env: 
          gloop: fig2
          bloop: test3
        run: |
          echo ${{secrets.GITHUB_TEST_ENV}}